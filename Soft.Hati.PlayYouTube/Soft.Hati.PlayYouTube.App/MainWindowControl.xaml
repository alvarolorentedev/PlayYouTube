<UserControl x:Class="Soft.Hati.PlayYouTube.App.MainWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Soft.Hati.PlayYouTube.App.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Name="MyToolWindow"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Margin="10" Text="Video ID" />
        <TextBox Grid.Column="1"
                 Margin="10"
                 Text="{Binding IDStringVideo}" />
        <Button Grid.Column="2"
                Width="100"
                Margin="10"
                Command="{Binding GoCommand}"
                Content="Go" />
        <controls:YoutubeVideoViewer Grid.Row="1"
                                     Grid.ColumnSpan="3"
                                     VideoID="{Binding IDVideo}" />
    </Grid>
</UserControl>
